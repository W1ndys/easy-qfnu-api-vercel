# easy-qfnu-api-go 开发规范

## 1. 工程目录结构 (Project Layout)

本项目遵循 Go 社区标准工程结构 (Standard Go Project Layout)，强调职责分离与模块化。

```text
easy-qfnu-api-go/
├── api/                    # 【接口层】Handler 处理函数，负责参数解析与响应封装
│   └── v1/                 # 版本控制，如 grade_handler.go
├── common/                 # 【公共层】通用工具
│   └── response/           # 统一响应封装 (response.go)
├── middleware/             # 【中间件】全局拦截器 (如 CORS, Auth, Logger)
├── model/                  # 【模型层】数据结构定义 (Struct)，对应数据库或下游返回
│   └── grade.go            # 成绩结构体
├── service/                # 【逻辑层】核心业务逻辑、爬虫、数据清洗
│   └── grade_service.go    # 成绩抓取与解析逻辑
├── web/                    # 【前端层】Go 模板 + 静态资源
│   ├── templates/          # Go HTML 模板文件
│   │   ├── layouts/        # 布局模板
│   │   │   └── base.html   # 基础布局（引入CDN、公共头尾）
│   │   ├── index.html      # 首页（功能导航页）
│   │   └── grade.html      # 成绩查询页面
│   └── static/             # 静态资源
│       ├── js/             # JavaScript 文件
│       │   ├── api/        # API 模块封装
│       │   │   ├── index.js    # API 统一导出
│       │   │   ├── request.js  # Axios 请求封装
│       │   │   └── grade.js    # 成绩相关 API
│       │   ├── utils/      # 工具函数
│       │   │   └── cookie.js   # Cookie 处理工具
│       │   └── pages/      # 页面逻辑
│       │       └── grade.js    # 成绩页面逻辑
│       └── css/            # 自定义样式（如有）
│           └── custom.css
├── go.mod                  # 依赖管理文件
└── main.go                 # 程序入口，负责路由注册与组件装配
```

---

## 2. 接口交互协议 (API Protocol)

- **传输协议**: HTTP/1.1
- **请求格式**:
- GET 请求参数使用 Query String
- POST 请求参数推荐使用 `application/json` 或 `application/x-www-form-urlencoded`

- **响应格式**: 统一使用 `application/json; charset=utf-8`
- **字符编码**: 内部统一处理为 UTF-8，对上游 GBK 编码进行透明转换。

---

## 3. 请求规范 (Request Standards)

### 3.1 鉴权 (Authentication)

本系统为无状态代理，不存储用户敏感信息。教务系统的 Cookie (Session ID) 必须通过 **HTTP Header** 传递，禁止在 URL 中明文传输。

| 参数名            | 位置   | 必填 | 说明                         | 示例                                 |
| ----------------- | ------ | ---- | ---------------------------- | ------------------------------------ |
| **Authorization** | Header | 是   | 教务系统的完整 Cookie 字符串 | `JSESSIONID=abc123456; SERVERID=...` |

> **安全提示**: 使用 Header 传递 Authorization 可避免 Authorization 被记录在服务器日志、代理服务器日志或浏览器历史记录中。

### 3.2 参数传递

- **资源定位**: 使用 Path 参数，如 `/api/users/:id`
- **筛选与分页**: 使用 Query 参数，如 `/api/grades?term=2025-2026-1`
- **表单提交**: 使用 Body 参数。

---

## 4. 响应规范 (Response Standards)

所有 API 必须返回统一的 JSON 结构（信封模式），禁止直接返回数组或裸数据。

### 4.1 统一结构定义

```go
type Response struct {
    Code int         `json:"code"` // 业务状态码：0-成功，非0-失败
    Msg  string      `json:"msg"`  // 提示信息：用于前端弹窗
    Data interface{} `json:"data"` // 业务数据：可以是 Object, Array 或 null
}

```

### 4.2 成功响应示例 (HTTP 200)

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "list": [
      {
        "course_name": "高等数学",
        "score": "95"
      }
    ],
    "count": 1
  }
}
```

### 4.3 失败响应示例 (HTTP 200)

注意：业务逻辑错误（如 Cookie 失效）通常返回 HTTP 200，通过 `code` 区分。

```json
{
  "code": 401,
  "msg": "Cookie 已失效，请重新登录",
  "data": null
}
```

---

## 5. 开发流程规范 (Development Workflow)

新增一个功能（例如：查询课表）的步骤：

1. **Model 层**:

- 在 `model/` 下新建 `schedule.go`。
- 定义 `Schedule` 结构体，字段首字母**必须大写**，并添加 `json` tag（推荐蛇形命名 `json:"course_name"`）。

2. **Service 层**:

- 在 `service/` 下新建 `schedule_service.go`。
- 编写 `FetchSchedule(cookie string)` 函数。
- 负责 Resty 请求、HTML 解析 (Goquery) 和数据清洗。
- **错误处理**: 遇到错误直接 `return nil, err`，不要在 Service 层吞掉错误。

3. **API 层**:

- 在 `api/v1/` 下新建 `schedule_handler.go`。
- 编写 `GetSchedule(c *gin.Context)`。
- 解析 Header 中的 `Authorization`。
- 调用 Service，处理 error，使用 `response.Success` 或 `response.Fail` 返回结果。

4. **Main 入口**:

- 在 `main.go` 中注册路由：`r.GET("/api/schedule", v1.GetSchedule)`。

---

## 6. 错误代码表 (Error Codes)

| Code    | 说明                  | 处理建议                            |
| ------- | --------------------- | ----------------------------------- |
| **0**   | **成功**              | 正常展示 Data                       |
| **1**   | **通用错误**          | 弹窗提示 Msg 内容                   |
| **401** | **未授权/Cookie失效** | 跳转至登录页引导用户重新输入 Cookie |
| **502** | **上游错误**          | 教务系统无响应或连接超时            |
| **500** | **解析错误**          | HTML 结构变更，需维护解析逻辑       |

---

## 7. 前端开发规范 (Frontend Standards)

### 7.1 技术栈

| 技术        | 说明             | 引入方式                                                                     |
| ----------- | ---------------- | ---------------------------------------------------------------------------- |
| TailwindCSS | CSS 框架         | CLI 编译，输出到 `/static/css/tailwind.css`                                  |
| Alpine.js   | 轻量级响应式框架 | `<script defer src="https://unpkg.com/alpinejs@3/dist/cdn.min.js"></script>` |
| Axios       | HTTP 请求库      | `<script src="https://unpkg.com/axios/dist/axios.min.js"></script>`          |
| Go Template | 服务端模板渲染   | 内置                                                                         |

#### TailwindCSS CLI 使用说明

```bash
# 安装 TailwindCSS CLI
npm install -D tailwindcss

# 初始化配置文件
npx tailwindcss init

# 开发模式（监听文件变化）
npx tailwindcss -i ./web/static/css/input.css -o ./web/static/css/tailwind.css --watch

# 生产模式（压缩输出）
npx tailwindcss -i ./web/static/css/input.css -o ./web/static/css/tailwind.css --minify
```

### 7.2 页面结构

所有页面基于 `layouts/base.html` 布局模板：

```html
<!-- web/templates/layouts/base.html -->
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{.Title}} - 卷卷的小工具库</title>
    <!-- TailwindCSS (CLI 编译输出) -->
    <link rel="stylesheet" href="/static/css/tailwind.css" />
  </head>
  <body class="bg-gray-100 min-h-screen">
    {{template "content" .}}

    <!-- Alpine.js -->
    <script defer src="https://unpkg.com/alpinejs@3/dist/cdn.min.js"></script>
    <!-- Axios -->
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <!-- API 模块 -->
    <script src="/static/js/api/request.js"></script>
    <script src="/static/js/api/index.js"></script>
    {{block "scripts" .}}{{end}}
  </body>
</html>
```

### 7.3 前端 API 封装规范

#### 7.3.1 请求封装 (request.js)

```javascript
// web/static/js/api/request.js
const request = axios.create({
  baseURL: "/api",
  timeout: 30000,
  headers: {
    "Content-Type": "application/json",
  },
});

// 请求拦截器：自动附加 Cookie
request.interceptors.request.use((config) => {
  const Authorization = localStorage.getItem("qfnu_cookie") || "";
  if (Authorization) {
    config.headers["Authorization"] = Authorization;
  }
  return config;
});

// 响应拦截器：统一处理错误
request.interceptors.response.use(
  (response) => {
    const res = response.data;
    if (res.code !== 0) {
      // 使用 Alpine.js 事件派发通知 Toast 组件
      window.dispatchEvent(
        new CustomEvent("show-toast", {
          detail: { message: res.msg, type: "error" },
        }),
      );
      if (res.code === 401) {
        // Cookie 失效，清除并提示
        localStorage.removeItem("qfnu_cookie");
      }
      return Promise.reject(res);
    }
    return res;
  },
  (error) => {
    window.dispatchEvent(
      new CustomEvent("show-toast", {
        detail: { message: "网络错误，请稍后重试", type: "error" },
      }),
    );
    return Promise.reject(error);
  },
);
```

#### 7.3.2 API 模块化 (按功能拆分)

```javascript
// web/static/js/api/grade.js
const gradeApi = {
  // 获取成绩列表
  getList(params = {}) {
    return request.get("/v1/grade", { params });
  },
  // 获取成绩详情
  getDetail(id) {
    return request.get(`/v1/grade/${id}`);
  },
};

// web/static/js/api/schedule.js
const scheduleApi = {
  // 获取课表
  getSchedule(params = {}) {
    return request.get("/v1/schedule", { params });
  },
};
```

#### 7.3.3 统一导出 (index.js)

```javascript
// web/static/js/api/index.js
const api = {
  grade: gradeApi,
  schedule: scheduleApi,
  // 新模块在此注册
};
```

### 7.4 Cookie 输入交互规范

每个功能页面必须包含 Cookie 输入组件：

Cookie 隐私保护

- 添加亚克力模糊效果 CSS
- 失焦时自动模糊（blur: 6px + opacity: 0.7）
- 悬停或聚焦时恢复清晰

```html
<!-- Cookie 输入区域 (使用 Alpine.js) -->
<div class="p-4 bg-white rounded-lg shadow mb-4" x-data="cookieForm()">
  <label class="block text-sm font-medium text-gray-700 mb-2">Cookie</label>
  <textarea
    x-model="cookie"
    class="w-full border rounded-lg p-3 transition-all duration-300 blur-[4px] focus:blur-none hover:blur-none opacity-60 focus:opacity-100"
    rows="3"
    placeholder="请粘贴教务系统 Cookie"
    @blur="saveCookie"></textarea>
  <button
    type="button"
    class="w-full mt-3 bg-[#885021] text-white py-3 rounded-xl font-semibold transition-all active:bg-[#5D3615] active:scale-[0.98]"
    @click="handleSubmit"
    :disabled="loading">
    <span x-show="!loading">查询</span>
    <span x-show="loading">查询中...</span>
  </button>
</div>
```

```javascript
// Cookie 处理逻辑 (Alpine.js 组件)
function cookieForm() {
  return {
    cookie: localStorage.getItem("qfnu_cookie") || "",
    loading: false,

    saveCookie() {
      if (this.cookie && this.cookie.trim()) {
        localStorage.setItem("qfnu_cookie", this.cookie.trim());
      }
    },

    getCookie() {
      return localStorage.getItem("qfnu_cookie") || "";
    },

    clearCookie() {
      localStorage.removeItem("qfnu_cookie");
      this.cookie = "";
    },

    async handleSubmit() {
      if (!this.cookie.trim()) {
        window.dispatchEvent(
          new CustomEvent("show-toast", {
            detail: { message: "请输入 Cookie", type: "warning" },
          }),
        );
        return;
      }
      this.saveCookie();
      this.loading = true;
      // 具体查询逻辑由各页面实现
    },
  };
}
```

---

## 8. 前端开发流程 (Frontend Workflow)

新增一个功能页面（例如：课表查询）的步骤：

### 8.1 创建 API 模块

```javascript
// web/static/js/api/schedule.js
const scheduleApi = {
  getSchedule(term) {
    return request.get("/v1/schedule", { params: { term } });
  },
};
```

在 `index.js` 中注册：

```javascript
const api = {
  grade: gradeApi,
  schedule: scheduleApi, // 新增
};
```

### 8.2 创建页面模板

```html
<!-- web/templates/schedule.html -->
{{template "layouts/base.html" .}} {{define "content"}}
<div class="container mx-auto p-4 max-w-lg" x-data="schedulePage()">
  <h1 class="text-xl font-bold mb-4">课表查询</h1>

  <!-- Cookie 输入 -->
  <div class="bg-white rounded-lg p-4 shadow">
    <textarea
      x-model="cookie"
      class="w-full border rounded-lg p-3 transition-all duration-300 blur-[4px] focus:blur-none hover:blur-none opacity-60 focus:opacity-100"
      rows="3"
      placeholder="请粘贴 Cookie"
      @blur="saveCookie"></textarea>
    <button
      @click="handleQuery"
      :disabled="loading"
      class="w-full mt-3 bg-[#885021] text-white py-3 rounded-xl font-semibold transition-all active:bg-[#5D3615] active:scale-[0.98] disabled:opacity-50">
      <span x-show="!loading">查询课表</span>
      <span x-show="loading">查询中...</span>
    </button>
  </div>

  <!-- 结果展示区 -->
  <div class="mt-4" x-show="scheduleData" x-transition>
    <template x-for="item in scheduleData" :key="item.id">
      <div class="bg-white rounded-lg p-4 shadow mb-3">
        <div class="font-medium" x-text="item.course_name"></div>
      </div>
    </template>
  </div>
</div>
{{end}} {{define "scripts"}}
<script src="/static/js/api/schedule.js"></script>
<script src="/static/js/pages/schedule.js"></script>
{{end}}
```

### 8.3 创建页面逻辑

```javascript
// web/static/js/pages/schedule.js
function schedulePage() {
  return {
    cookie: localStorage.getItem("qfnu_cookie") || "",
    loading: false,
    scheduleData: null,

    saveCookie() {
      if (this.cookie && this.cookie.trim()) {
        localStorage.setItem("qfnu_cookie", this.cookie.trim());
      }
    },

    async handleQuery() {
      if (!this.cookie.trim()) {
        window.dispatchEvent(
          new CustomEvent("show-toast", {
            detail: { message: "请输入 Cookie", type: "warning" },
          }),
        );
        return;
      }

      this.saveCookie();
      this.loading = true;

      try {
        const res = await api.schedule.getSchedule();
        this.scheduleData = res.data;
      } catch (err) {
        console.error(err);
      } finally {
        this.loading = false;
      }
    },
  };
}
```

### 8.4 注册路由 (Go)

```go
// main.go
r.GET("/schedule", func(c *gin.Context) {
    c.HTML(http.StatusOK, "schedule.html", gin.H{
        "Title": "课表查询",
    })
})
```

---

## 9. 命名规范 (Naming Conventions)

### 9.1 文件命名

| 类型        | 命名规则           | 示例                       |
| ----------- | ------------------ | -------------------------- |
| Go 文件     | 小写下划线         | `grade_service.go`         |
| HTML 模板   | 小写连字符或下划线 | `grade.html`               |
| JS API 模块 | 小写               | `grade.js`                 |
| JS 页面逻辑 | 小写               | `grade.js` (在 pages 目录) |

### 9.2 变量命名

| 语言       | 规则          | 示例                        |
| ---------- | ------------- | --------------------------- |
| Go         | 驼峰命名      | `courseList`, `GetGrade`    |
| JavaScript | 小驼峰        | `gradeApi`, `handleSubmit`  |
| JSON 字段  | 蛇形命名      | `course_name`, `created_at` |
| CSS 类名   | Tailwind 优先 | `bg-blue-500`, `text-lg`    |

---

## 10. 首页导航规范 (Index Page)

首页作为功能入口，需清晰展示所有可用功能：

```html
<!-- web/templates/index.html -->
{{template "layouts/base.html" .}} {{define "content"}}
<div class="container mx-auto p-4 max-w-lg">
  <h1 class="text-2xl font-bold text-center mb-6">QFNU 教务助手</h1>

  <div class="grid grid-cols-2 gap-4">
    <a href="/grade" class="bg-white rounded-lg p-4 shadow text-center">
      <div class="text-3xl mb-2">📊</div>
      <div class="font-medium">成绩查询</div>
    </a>
    <a href="/schedule" class="bg-white rounded-lg p-4 shadow text-center">
      <div class="text-3xl mb-2">📅</div>
      <div class="font-medium">课表查询</div>
    </a>
    <!-- 更多功能卡片 -->
  </div>
</div>
{{end}}
```
